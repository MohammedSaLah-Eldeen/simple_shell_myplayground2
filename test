#include "main.h"

int main(int ac, char **av)
{
  int shell = 1;
  int errors = 0;
  char **command;
  int i;
  ac = ac;

  while (shell)
    {
      command = fetch_command(&shell, &errors, av);
      if (command != NULL)
	{
	  for (i = 0; command[i] != NULL; i++)
	    printf("%s\n", command[i]);
	}
    }

  return (0);
}
